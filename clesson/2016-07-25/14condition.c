
#include <stdio.h>

int main(void)
{
	int i = 100 ; 
	int b = 0;  //局部变量没赋值则随机
	int c ; 
	//条件||  如果前面语句为真，后面语句不执行
	//c = (i = 200) || (b = 100) ;  //条件语句
	//条件&&  如果前面为假，后面不执行
	//c = (i = 0) && (b = 100) ;  //条件语句

	//!!    不会改变真假关系，但是结果只有0/1

	printf("i : %d  b:%d  c:%d \n" , i , b , c );


	return 0 ; 
}
