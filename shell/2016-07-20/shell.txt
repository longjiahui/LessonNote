
外部命令   第三方实现，不是shell内部自带,可以找到实际的可执行文件
内部命令   Shell自带的，内部实现

区分一个内部命令及外部命令 
whereis    command
type       command


在shell内，如果想要找印一个变量的值必须给钱$

shell内部，变量都是万能类型
shell内部，变量赋值不可以加空格   空格 认为是一条命令

shell文件必须以.sh结尾

这个是做测试的，不要看
